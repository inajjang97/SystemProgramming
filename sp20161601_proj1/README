Makefile에서 컴파일은 gcc를 통해 수행되도록 하였으며,
gcc 모드는 -g -Wall로 설정하였다.

컴파일을 위해 make입력하고,  
컴파일 수행시20161601.out 파일이 생성되며, 
./20161601.out 를 입력하여 실행시킨다.

prompt 최대 입력 문자 길이는 256으로 
이를 넘어가면 제대로된 입력 형식이라도 수행되지 않는다. 
