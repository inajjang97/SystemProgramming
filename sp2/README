< 특별하게 오류 처리한 경우들>
X'  03' 	; ' '사이의 공백 
x   '03'	; x(or c) 와 ' 사이의 공백

BYTE C'ABC' BYTE로 선언한 문자열상수의 최대 길이는 30 (30byte)
이번 프로젝트에서는 asm 파일을 읽어와 obj code를 생성하여 obj, lst 파일을 만드는 것까지 수행하였음
(format 2에서 register 값을 바꿔준 경우, obj 코드는 생성하지만 그 연산 결과가 수행되지는 않음!)
(따라서 index mode, 시작 인덱스는 0뿐임(x 레지스터 초기 값이0이고, 이 값이 바뀌지 않음)	

